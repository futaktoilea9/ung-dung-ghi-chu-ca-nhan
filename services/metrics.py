from __future__ import annotations


def recent_note_search_summary() -> dict[str, object]:
    return {
        'recent_queries': ['meeting', 'idea', 'weekly plan'],
        'saved_notes': 42,
        'tag_count': 9,
    }
